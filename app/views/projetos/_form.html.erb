<%= form_with(model: projeto, local: true) do |form| %>
  <% if projeto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(projeto.errors.count, "error") %> prohibited this projeto from being saved:</h2>

      <ul>
      <% projeto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_field :titulo, id: :projeto_titulo %>
  </div>

  <div class="field">
    <%= form.label :username_id %>
    <%= form.text_field :username_id, id: :projeto_username_id %>
  </div>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao, id: :projeto_descricao %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link, id: :projeto_link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>
